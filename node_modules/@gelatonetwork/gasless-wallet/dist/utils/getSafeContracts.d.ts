export interface SafeAddressBook {
    gnosisSafe: string;
    gnosisSafeProxyFactory: string;
    fallbackHandler: string;
}
export declare const getSafeContractAddresses: (chainId: number) => SafeAddressBook;
